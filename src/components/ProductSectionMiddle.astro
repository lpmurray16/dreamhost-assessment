---

---

<div class="product">
    <div class="product__section">
        <div class="product__section-left">
            <h2>Get Your Website Live Today</h2>
            <p>Stack the deck in your favor by combing WordPress,
                the world's most popular website platform, with
                DreamHost's optimized web hosting.
            </p>
            <div class="product__cta-wrapper">
                <a href="#" class="product__cta bttn bttn-primary">
                    Get Started
                </a>
                <span>Starting at just $2.59/mo</span>
            </div>
        </div>
        <div class="product__section-right">
            <div class="product__section-right-image">
                <img src="/assets/images/image_3.png" alt="Mobile Demo">
            </div>
        </div>
    </div>
</div>

<style lang="scss">
    .product {
        margin: clamp(2rem, 3vw, 4rem) auto;
        padding: 10rem 0;

        @media screen and (max-width: 1024px) {
            margin: 0;
            padding: 0;
        }


        &__section {
            background-color: var(--dark-blue);
            display: flex;
            position: relative;
            height: 700px;
            justify-content: space-between;
            align-items: center;

            &-left {
                flex: 1;
                padding-left: 7.8vw;

                h2,
                p,
                span {
                    color: var(--white);
                }

                h2 {
                    font-size: clamp(64px, 3.7vw, 88px);
                    font-weight: bold;
                    line-height: 80px;
                    margin-bottom: 2rem;
                    width: 12ch;

                    @media screen and (max-width: 1024px) {
                        font-size: clamp(48px, 2.5vw, 64px);
                        line-height: 80px;
                    }

                    @media screen and (max-width: 768px) {
                        width: 100%;
                        max-width: 18ch;
                        font-size: clamp(42px, 2.6vw, 64px);
                    }
                }

                p {
                    font-size: clamp(14px, 1.5vw, 24px);
                    line-height: 40px;
                    margin-bottom: 3rem;
                    font-weight: 500;

                    @media screen and (max-width: 1024px) {
                        font-size: clamp(24px, 3vw, 32px);
                        line-height: 40px;
                    }

                    @media screen and (max-width: 768px) {
                        max-width: 100%;
                        padding-right: 2rem;
                    }
                }

                @media screen and (max-width: 768px) {
                    padding-left: 2rem;
                }
            }

            &-right {
                flex: 1;
                padding-right: 7.8vw;
                position: relative;
                z-index: 1;

                @media screen and (max-width: 1024px) {
                    padding-right: 0;
                }

                &-image {
                    position: absolute;
                    top: -24.5vw;
                    right: -18%;
                    width: 100%;
                    height: 100%;
                    z-index: 1;

                    @media screen and (max-width: 1024px) {
                        position: relative;
                        inset: 0;
                        display: flex;
                        justify-content: center;
                        align-items: center;
                    }


                    img {
                        width: 60%;
                        min-width: 365px;
                        height: auto;
                        max-width: 100%;
                        object-fit: cover;
                    }
                }

                @media screen and (max-width: 768px) {
                    display: none;
                }
            }
        }

        &__cta-wrapper {
            display: inline-flex;
            align-items: center;

            @media screen and (max-width: 768px) {
                display: flex;
                flex-direction: column;
                align-items: flex-start;
                justify-content: flex-start;
            }

            span {
                margin-left: 2rem;
                font-size: clamp(16px, 3vw, 20px);

                @media screen and (max-width: 768px) {
                    margin-left: 0;
                    margin-top: 2rem;
                }
            }
        }
    }
</style>